const apiName = "Login API";
const statusCode = 200;
const responseTime = 1200;

const message = `${apiName} returned status ${statusCode} in ${responseTime} ms`;
//Login API returned status 200 in 1200 ms

console.log(message);
